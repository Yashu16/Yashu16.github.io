/* TOC rules (if you already have them, leave those as-is) */
.toc {
  float: right;
  width: 220px;
  margin-left: 1rem;
  margin-top: 0;
  font-size: 0.95rem;
  line-height: 1.4;
  border-left: 3px solid #eee;
  padding-left: 0.75rem;
  position: sticky;
  top: 1rem;
}
@media (max-width: 900px) {
  .toc {
    float: none;
    width: auto;
    position: static;
    border-left: none;
    padding-left: 0;
    margin-bottom: 1rem;
  }
}
.toc a { color: inherit; text-decoration: none; }
.toc a:hover { text-decoration: underline; }

/* Hero image at the top of the page */
.hero-header {
  width: 100%;
  margin-bottom: 1.25rem;
  overflow: hidden; /* keep image nicely clipped if using object-fit */
  display: block;
}

.hero-img {
  display: block;
  width: 100%;
  height: auto;           /* maintain aspect ratio on narrow screens */
  max-height: 420px;     /* limit vertical size on large screens */
  object-fit: cover;     /* crop to fill the box nicely */
}

/* Optional: add a darker overlay and centered title inside header */
/* .hero-header { position: relative; }
   .hero-header::after {
     content: "";
     position: absolute;
     inset: 0;
     background: rgba(0,0,0,0.25); 
   }
   .hero-title { position: absolute; left: 1rem; bottom: 1rem; color: white; } */

@media (max-width: 480px) {
  .hero-img { max-height: 260px; }
}
